apiVersion: v1
kind: Service
metadata:
  name: linguistai-client
  namespace: linguistai
  labels:
    app: linguistai
    component: client
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: linguistai
    component: client
